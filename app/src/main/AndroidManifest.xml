<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.planboxone" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:logo="@drawable/ic_logo"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="微计划"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activity.SettingActivity"
            android:label="设置"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@android:style/Theme.Holo.NoActionBar" >
            android:label="@null">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.GuideActivity"
            android:theme="@android:style/Theme.Holo.NoActionBar" />
        <activity android:name=".test.ModifyPassActivity" />
        <activity android:name=".test.OpenPsdActivity" />
        <activity android:name=".test.ClosePsdActivity" />
        <activity android:name=".test.SetPsdActivity" />
        <activity android:name=".activity.AboutUsActivity" />
        <activity
            android:name=".test.DateTimePickActivity"
            android:label="@string/title_activity_date_time_pick" />
        <activity
            android:name=".activity.MyWritePlanActivity"
            android:label="@string/title_activity_my_write_plan" >
        </activity>
    </application>

    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>
